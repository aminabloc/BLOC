<p class ="user">
		<%= @current_user.name %>

	<h1 class="school">
		<%= @user.school %>
	</h1>

	<h1 class="school">
		<%= @user.school %>
	</h1>

	<h1 class="school">
			<%= @user.major %>
		</h1>
	<h1 class="school">
		<%= @user.experience %>
	</h1>

	<h1 class="school">
		<%= @user.skills %>
	</h1>


	<p class="date">
		Profile update <%= time_ago_in_words(@profile.created_at) %> Ago
	
	<% if user_signed_in? %>
			| <%= link_to 'Edit', edit_user_registration_path(@user) %> 
			
		<% end %>
	</p>

	<p class="body" style="font-size: 1.2em; margin: 2em; float:left" >
 
	