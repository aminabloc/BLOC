<%=  form_for([@post, @post.comments.build], html: { multipart: true }) do |f| %>
	<p>
		<%= f.label :body %><br>
    <%= f.text_area :name %><br> 
    <%= f.file_field :image %>
	</p>

	<br>
	<p>
		<%= f.submit %>
	</p>
	
<% end %>
